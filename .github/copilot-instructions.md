# Copilot Instructions

## Project Guidelines
- Code formatting preferences: always use braces for single-line statements; keep lines under 180 characters but avoid unnecessary line breaks (prefer longer single lines over wrapping); remove redundant/dead code as part of any change.