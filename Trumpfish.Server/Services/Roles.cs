namespace Trumpfish.Server.Services;

public static class Roles {

    /// <summary>The seeded developer account. Only granted by the seeder, never by self registration.</summary>
    public const string Admin = "Admin";
}
